# Объяснение Singleton паттерна через модульную систему JS

## Визуальное представление

```
┌─────────────────────────────────────────────────────────────┐
│  useWebSocketConnection.ts (Модуль загружается ОДИН РАЗ)   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  // Эти переменные создаются ОДИН РАЗ при загрузке модуля │
│  const wsUrl = ref('')              ◄─────┐                │
│  const status = ref('CLOSED')             │                │
│  const isInitialized = ref(false)         │                │
│  const { send, open, close } = useWebSocket(...)           │
│                                            │                │
│  export const useWebSocketConnection = () => {             │
│      return { wsUrl, status, ... }  ◄──────┼────┐          │
│  }                                          │    │          │
│                                             │    │          │
└─────────────────────────────────────────────┼────┼──────────┘
                                              │    │
                ┌─────────────────────────────┘    │
                │                                  │
    ┌───────────▼──────────┐          ┌───────────▼──────────┐
    │      App.vue         │          │   ChatComponent.vue  │
    ├──────────────────────┤          ├──────────────────────┤
    │ import { use... }    │          │ import { use... }    │
    │                      │          │                      │
    │ const { open } =     │          │ const { status } =   │
    │   useWebSocket...()  │          │   useWebSocket...()  │
    │                      │          │                      │
    │ open('ws://...')     │          │ watch(status, ...)   │
    │    │                 │          │    │                 │
    │    └─────────────────┼──────────┼────┘                 │
    │                      │          │                      │
    │  Оба компонента      │          │  Оба компонента      │
    │  работают с ОДНИМ    │◄────────►│  видят ОДНО          │
    │  И ТЕМ ЖЕ wsUrl      │          │  И ТО ЖЕ status      │
    └──────────────────────┘          └──────────────────────┘
```

## Пошаговое объяснение

### Шаг 1: Первый импорт модуля (App.vue загружается)

```typescript
// App.vue загружается первым
import { useWebSocketConnection } from '@/composables/useWebSocketConnection'

// ⬇️ JavaScript выполняет весь код модуля useWebSocketConnection.ts
// ⬇️ Создаются все переменные на уровне модуля:

const wsUrl = ref('')                    // ← Создано в памяти
const status = ref('CLOSED')             // ← Создано в памяти
const isInitialized = ref(false)         // ← Создано в памяти
const { send, open, close } = useWebSocket(...) // ← WebSocket инициализирован

// ⬇️ Функция useWebSocketConnection создана и экспортирована
// ⬇️ Модуль кешируется в системе модулей JavaScript
```

### Шаг 2: Использование в App.vue

```typescript
// App.vue
const { websocketOpen, isConnected } = useWebSocketConnection({
    onReauthorize: handleReauth
})

// ⬆️ Функция просто возвращает ссылки на уже созданные объекты
// ⬆️ Никакие новые WebSocket соединения НЕ создаются
```

### Шаг 3: Второй импорт модуля (ChatComponent.vue загружается)

```typescript
// ChatComponent.vue
import { useWebSocketConnection } from '@/composables/useWebSocketConnection'

// ⬆️ JavaScript НЕ выполняет код модуля заново!
// ⬆️ Вместо этого возвращается КЕШИРОВАННАЯ версия модуля
// ⬆️ Все переменные уже существуют в памяти
```

### Шаг 4: Использование в ChatComponent.vue

```typescript
// ChatComponent.vue
const { isConnected, sendMessage } = useWebSocketConnection()

// ⬆️ Получаем ссылки на ТЕ ЖЕ САМЫЕ объекты, что и в App.vue
// ⬆️ isConnected.value - это ОДИН И ТОТ ЖЕ ref
// ⬆️ sendMessage - это ОДНА И ТА ЖЕ функция
```

### Шаг 5: Изменение состояния

```typescript
// В App.vue
websocketOpen('wss://server.com')
// ⬇️ Изменяет wsUrl.value и вызывает open()

// В ChatComponent.vue (автоматически!)
watch(isConnected, (value) => {
    console.log(value) // true - реактивность работает!
})
// ⬆️ Видит изменения, потому что это ТОТ ЖЕ ref!
```

## Сравнение с традиционным Singleton

### ❌ Традиционный подход (избыточный)

```typescript
// Нужна переменная для хранения инстанса
let instance: WebSocketInstance | null = null

// Нужна функция-фабрика
function createInstance() {
    const wsUrl = ref('')
    const status = ref('CLOSED')
    // ... много кода
    return { wsUrl, status, ... }
}

// Нужна проверка на КАЖДОМ вызове
export const useWebSocketConnection = () => {
    if (!instance) {  // ← Проверка каждый раз!
        instance = createInstance()
    }
    return instance
}

// Итого: 3 уровня абстракции, проверка на каждом вызове
```

### ✅ Модульный подход (элегантный)

```typescript
// Переменные создаются на уровне модуля
const wsUrl = ref('')
const status = ref('CLOSED')
// ... код

// Просто возвращаем ссылки
export const useWebSocketConnection = () => {
    return { wsUrl, status, ... }
}

// Итого: 1 уровень, никаких проверок, естественно для JS
```

## Ключевые моменты

### 1. Модули в JavaScript - это singleton по умолчанию

```javascript
// module.js
console.log('Этот код выполнится ТОЛЬКО ОДИН РАЗ')
export const data = { count: 0 }

// app.js
import { data } from './module.js'
data.count++ // 1

// component.js
import { data } from './module.js'
console.log(data.count) // 1 (то же самое значение!)
```

### 2. Vue Reactivity работает через ссылки

```javascript
// Создаем ref один раз
const count = ref(0)

// Компонент A
const { count: countA } = { count }
countA.value++ // Изменяет оригинальный ref

// Компонент B
const { count: countB } = { count }
console.log(countB.value) // 1 (видит изменения!)
```

### 3. Не нужно создавать новые объекты

```javascript
// ❌ Плохо - создаем новый объект каждый раз
export const useData = () => {
    return { data: ref(0) } // Новый ref каждый раз!
}

// ✅ Хорошо - возвращаем ссылку на существующий
const data = ref(0)
export const useData = () => {
    return { data } // Тот же ref
}
```

## Преимущества модульного подхода

1. **Производительность**: Нет проверок `if (!instance)` на каждом вызове
2. **Простота**: Меньше кода, меньше абстракций
3. **Читаемость**: Линейный код, легко понять
4. **Естественность**: Используем встроенные возможности JS
5. **Надежность**: Система модулей - стандарт ES6
6. **Типизация**: TypeScript лучше понимает такой код

## Когда это НЕ работает?

### Server-Side Rendering (SSR)

```javascript
// ⚠️ В SSR каждый запрос должен иметь свой инстанс
// Модульный singleton будет общим для всех запросов!

// Решение: использовать provide/inject или контекст запроса
```

### Hot Module Replacement (HMR)

```javascript
// ⚠️ При HMR в dev режиме модуль может перезагрузиться
// Состояние может быть потеряно

// Решение: это нормально для dev режима
// В production HMR не используется
```

## Заключение

Модульный подход к singleton в JavaScript - это:
- ✅ Естественный способ работы с модулями
- ✅ Рекомендуемый подход для Vue 3 composables
- ✅ Более простой и понятный код
- ✅ Лучшая производительность
- ✅ Меньше возможностей для ошибок

**Используйте возможности языка, а не боритесь с ними!** 🚀

